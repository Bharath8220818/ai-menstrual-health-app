Place the app launcher image here as `app_icon.png`.

Steps:
1. Replace this file by the provided logo image and name it `app_icon.png`.
2. Then run:

```powershell
flutter pub get
flutter pub run flutter_launcher_icons:main
```

That will generate Android mipmap icons and the iOS AppIcon set.

If you want, I can run the commands after you confirm the image is present at this path.
